extends layout

block content
  h1= title
  p Welcome to #{title}
  p Learning to create servers using pug templates
  button(type="button", class="btn btn-primary", onclick="doBump()") Press Me for the contents
  div(id="bump") Bump the count
  script.
   var count=0;
   var bump=1;
   function doBump(){
    count+=bump;
    bump+=1;
    document.getElementById("bump").innerHTML="Count "+count+" Bump "+bump;
   }
  a(href="umbrella") Umbrella page